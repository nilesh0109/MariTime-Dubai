(function() {
    'use strict';
    describe('module mTDubai', function() {

        it('should exist', function() {
            var mod = module('mTDubai');
            expect(mod).not.toEqual(null);
        });
    });
})();